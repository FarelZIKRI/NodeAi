[build]
  command = "cd apps/frontend && npm install --include=dev && npm run build"
  publish = "apps/frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback untuk React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
